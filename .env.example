# ===========================================
# BillHippo Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to git!
#
# HOW TO GET THESE VALUES:
# 1. Go to https://console.firebase.google.com
# 2. Select your project → Project Settings (gear icon)
# 3. Scroll to "Your apps" section → Web app config
# 4. Copy each value below
# ===========================================

# Firebase Configuration
VITE_FIREBASE_API_KEY=your-api-key-here
# Auth domain for Google sign-in. Leave blank to auto-detect from the hosting
# domain (recommended for Firebase Hosting with a custom domain). Set explicitly
# only for local development, e.g. your-project-id.firebaseapp.com
# IMPORTANT: The hosting domain must also be added to Firebase Console →
# Authentication → Settings → Authorized domains.
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-your-measurement-id
