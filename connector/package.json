{
  "name": "billhippo-tally-connector",
  "version": "0.4.10",
  "description": "BillHippo Desktop Connector — bridges Firestore and a local Tally Prime instance.",
  "author": "BillHippo",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --win",
    "pack": "npm run build && electron-builder --dir"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "electron-store": "^8.2.0",
    "electron-updater": "^6.3.9",
    "fast-xml-parser": "^4.4.0",
    "firebase": "^12.9.0"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  }
}
